# Machine

machine:
  node:
    version: 6

# Dependencies

dependencies:
  override:
    - npm install -g angular-cli
    - npm install

# Test Commands

test:
  override:
    - ng build
    - ng test --watch=false

# Safelist
general:
  branches:
    only:
      - master
